// https://github.com/thematters/matters-web/tree/develop/src/components/Dialog/Header
//
export interface HeaderProps {
  title: React.ReactNode;
  closeDialog: () => void;
}

const DialogHeader = (props: HeaderProps) => {
  return <span>DialogHeader</span>;
};

export default DialogHeader;
